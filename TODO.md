# TODO

## Priority

- [ ] **Monitor moon-phase indicator's continuous motion over several days** — `buildMoonPhaseSection` (script.js) computes `indicatorPercent` from the raw normalized `moon_phase` fraction (`normalizedPhase * 100`) rather than the discrete 0–8 bucket index, so the ▼ triangle is meant to glide a little further each day *within* the currently-highlighted icon's cell instead of jumping between 9 fixed stops. This depends on the two New Moon buckets in `getMoonPhaseRowIndex` being exactly half-width (6.25% of the cycle each, `p < 0.0625` and `p >= 0.9375`) and the other seven being exactly 12.5% each — matching the icon row's even 12.5%-per-icon spacing so the math lines up without any remapping. Verified analytically and against one day's data (Waxing Gibbous, indicator at 34.9% inside the expected 31.25–43.75% cell) — but only one snapshot. Watch over the next few days/weeks that: (1) the triangle keeps advancing smoothly through Waxing Gibbous → Full Moon → Waning Gibbous etc. as days pass, rather than sitting frozen or jumping; (2) it never visibly drifts outside the currently-highlighted icon's own cell; (3) at the cycle wrap it correctly teleports from the right edge (Old Moon) back to the left edge (New Moon) rather than sliding backwards through the whole row or glitching.
- [ ] **Dew point** — `dew_point_2m`, often a better "how muggy does it feel" indicator than relative humidity alone; could sit next to the humidity card.
- [ ] **Moon cycle dates** — show upcoming key phase dates (next Full Moon, next New Moon) instead of just today's phase. `daily.moon_phase` is already fetched across the 10-day forecast window, so the next crossing may already be computable from data already in hand; a crossing further out than 10 days would need a wider `forecast_days` request.
- [ ] **Resume golden/blue hour vertical-bar spacing/sizing** — the `.golden-hour-divider` bars (`style.css`) are currently color-matched to the background (invisible) rather than removed, specifically so the layout doesn't shift when this is picked back up. The reference for the desired final look is saved at `C:\Claude Code Projects\Golden hour.png`. Still unresolved from the last attempt: the bars' height relative to the time columns and their padding from the "Golden & blue hours" label didn't match that reference despite several rounds of tuning (line-height, border width/opacity, gap/padding) — worth a fresh look rather than more incremental tweaks.

## Optional

- [ ] **Wind gusts** — `wind_gusts_10m`, alongside the existing sustained wind speed card, since gustiness isn't visible from sustained speed alone.
- [ ] **Colorblind-friendly mode** — audit and fix color-only encodings across the app (a toggle, or auto via a preference, that adds a secondary cue — shape, pattern, or text — wherever color alone currently carries meaning). Concrete instance found so far: the golden/blue hour markers (currently 🔵/🟡, briefly tried 🔷/🔶) are same-shape dots distinguished only by fill color — blue/yellow specifically breaks for tritanopia (blue-yellow colorblindness, rare but real), and swapping to blue/orange diamonds didn't actually add a shape cue since both emoji are diamonds, just recolored. Other spots worth auditing: the Wind/UV/Air quality/Humidity/Pressure segmented bars (color-coded zones with no pattern/label backup beyond the marker's numeric value), and the 10-day forecast's blue precipitation-probability text (relies on blue vs. the surrounding brown, not a symbol). Needs a scope decision first — a real toggle vs. targeted incremental fixes done piecemeal.
- [ ] **Historical/seasonal comparison** — e.g. "4°C above the typical high for this date," using Open-Meteo's separate historical/climate API. Bigger lift than the others — a new API integration, not just a param addition.
